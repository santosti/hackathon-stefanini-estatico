import { OnInit, OnDestroy } from '@angular/core';
import { ToastMessage } from '../../Models/toast-message.models';
import { AngularBootstrapToastsService } from '../../angular-bootstrap-toasts.service';
export declare class ToastMessageComponent implements OnInit, OnDestroy {
    private toastsService;
    Toast: ToastMessage;
    currentDuration: number;
    progressLineWidth: string;
    private msInterval;
    private isMouseFocused;
    private durationTimer;
    constructor(toastsService: AngularBootstrapToastsService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    animationPause(): void;
    animationPlay(): void;
    onClick(): void;
    remove(confirmationResult?: boolean): void;
}
