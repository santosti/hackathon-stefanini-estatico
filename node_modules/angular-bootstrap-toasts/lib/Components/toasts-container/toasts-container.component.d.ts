import { OnInit, OnDestroy } from '@angular/core';
import { ToastMessage } from '../../Models/toast-message.models';
import { AngularBootstrapToastsService } from '../../angular-bootstrap-toasts.service';
import { PositionModel } from '../../Models/toast-container.models';
export declare class ToastsContainerComponent implements OnInit, OnDestroy {
    private toastsService;
    Placement: PositionModel;
    /** Width as CSS value */
    Width: string;
    Classes: string;
    private defaultMargin;
    MarginAsString: string;
    toastsList: ToastMessage[];
    private messagesListSubscription;
    constructor(toastsService: AngularBootstrapToastsService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private initParams;
}
